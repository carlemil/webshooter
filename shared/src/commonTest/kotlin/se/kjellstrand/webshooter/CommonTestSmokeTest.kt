package se.kjellstrand.webshooter

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * Trivial smoke test — its only job is to prove the commonTest source set
 * is wired up and runs on every configured KMP target.
 */
class CommonTestSmokeTest {

    @Test
    fun trueIsTrue() {
        assertEquals(true, true)
    }
}
